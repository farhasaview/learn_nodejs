var name = 'Febi';
var age = 24;
var hasHobbies = true;

function summarizeUser(userName, userAge, userHasHobby) {
  return (
    'Name is ' +
    userName +
    ', age is ' +
    userAge +
    ' and the user has hobbies: ' +
    userHasHobby
  );
}

console.log(summarizeUser(name, age, hasHobbies));

////////////////////////////////////////////////////////////////////////////////////////////////////////

var name1 = 'Aris';
var age1 = 23;
var hasHobbies1 = true;

function summarizeUser1(userName1, userAge1, userHasHobby1) {
  return (
  'Name is ' + userName1 +
  ' age is ' + userAge1 +
  ' and the user has hobbies: ' + userHasHobby1
  );
}

console.log(summarizeUser1(name1, age1, hasHobbies1));

/////////////////////////////////////////////////////////////////////////////////////////////////////////